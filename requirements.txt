# =============================================================================
# REQUIREMENTS.TXT - Python Dependencies
# =============================================================================
# This file lists all Python packages needed for this project.
#
# To install all dependencies, run:
#   pip install -r requirements.txt
#
# To update packages:
#   pip install --upgrade -r requirements.txt
# =============================================================================

# HTTP library for making API requests
# Used for: requests.get(), requests.post(), requests.put(), requests.delete()
requests>=2.28.0

# Testing framework
# Used for: pytest fixtures, assertions, test discovery
pytest>=7.0.0

# Pytest plugin for HTML report generation
# Used for: --html option to generate HTML reports
pytest-html>=4.0.0

# Pytest plugin for test dependencies
# Used for: @pytest.mark.dependency to chain tests
pytest-dependency>=0.5.0
